version: "3.8"

services:
  sshllm:
    build: .
    container_name: sshllm
    ports:
      - "2222:22"
    restart: always
    environment:
      LLM_PROVIDER: "${LLM_PROVIDER}"
      LLM_SERVER_URL: "${LLM_SERVER_URL}"
      LLM_MODEL: "${LLM_MODEL}"
    volumes:
      - ./log:/opt/sshllm/log
